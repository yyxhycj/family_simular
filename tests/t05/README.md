# T05 可选编辑器直接合同

运行：

```sh
/private/tmp/jiaye-t02-venv/bin/python tests/t05/run.py --out qa/t05/<新的目录名>
```

覆盖摘要直达世道/家底、仅改一项后返回摘要、人物临时副本的姓名/性别/偏向/初始安排、年龄导致的非法关系、删除标记取消/保存、跨页撤销、超分阻止开始，以及独立 Lua VM 的草案回读。

这是生产 Lua 模块配合声明式 UI 和 File 适配层的合同，不证明 UrhoX 实际绘制、触控、输入法、360/390/430 布局或真机。
