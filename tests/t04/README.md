# T04 开局摘要直接合同

运行：

```sh
/private/tmp/jiaye-t02-venv/bin/python tests/t04/run.py --out qa/t04/<新的目录名>
```

覆盖当前 Lua 模块的默认摘要字段绑定、首任族长初始安排、多件信物摘要、账本/点数/成员/信物明细往返、整户换一家与恢复、首次开始快照、旧局取消/确认替换、非法候选禁用，以及新 Lua VM 保存回读。

该合同使用声明式 UI 和 File 的边界适配层，不能证明 UrhoX 实际绘制、360/390/430 布局、触控、输入法、安全区或真机录屏；这些必须以当前提交的 Maker 引擎和设备证据另行验收。
