# T01｜收藏、完整家史、继任与保存保护

状态：**T01 功能验收通过：直接模块 6/6、Chrome 中的 Maker UrhoX 实际交互、状态/记录/界面/回读存档一致；录像已归档。真机未验收，T02–T11 未实施。**

基线 HEAD：`9532e4435bab5aea1e38f289e950208342becefb`。首次交付时为未提交的工作区修改；2026-09-16 续验已由 Maker 工具提交并构建，修复提交为 `ca5b5f2`。历史评审仍固定为 `546ff6cf7e4225632fe9871ba70188e6aa730e82`，T00 和首次验收原始证据未改写。

最终玩法及显示修复：`c2f24fd2dfeae9d889a6424992c57c121f5c75d7`；正式入口恢复并构建：`ca26af6cb6fb9dc9c18ca747f8f1de78edc9bb86`。Maker origin/main 与本地 HEAD 一致，入口为 `main.lua`。测试/报告/录像仍保存在本地，未单独推送 GitHub。

## 范围与修改文件

按交接手册只执行 T01，未开展 T02–T11。保持现有 Lua/UrhoX、凡世、价格及经济规则；本步不声称已经实现整户真随机或手机紧凑开局。

| 文件 | 本步实现 |
| --- | --- |
| `scripts/Jiaye/State.lua` | 取消 120 条日志截断；存档交替写入两份文件，成功需 boolean 写入结果和原文回读一致；按 `saveRevision` 选最近可读档；兼容现有无编号存档；坏档不静默覆盖；导出如实返回失败。 |
| `scripts/Jiaye/Simulation.lua` | 两个继任入口共用任命命令，同时结束前任任期、写新任期、关闭待决继任事件并记录继任人/年份；修复旧档“已任命但仍待决”，不重复任期或奖励。 |
| `scripts/Jiaye/App.lua` | 初始化读取 Profile 和旧 run；试配新局期间保留旧 run/草案；只有确认且写入成功后切换；可取消回旧局；保存失败常驻提示、重试/导出、阻止重复安排；年鉴每页 20 条并可直达最早记录；修正实测发现的新局确认、人物任命弹窗底色与文字对比度。 |
| `scripts/QA/T01.lua`、同名 `.meta` | 独立实际引擎验收入口，复用生产模块与真实 UI/File/cjson；仅三个存档文件名加 `t01_qa_` 前缀，隔离原家谱；夹具不作为玩家经历。正式入口不引用此文件。 |
| `tests/t01/{run.py,boundary.lua,cases.lua,README.md}` | 六组直接模块合同，真实 Lua 5.4、真实按钮回调、磁盘 File 适配和独立 VM 重启。 |
| `qa/t01/final/` | 最终结果、源码哈希、LSP 结果、隔离存档夹具。 |
| `qa/t01/engine-attempt/` | 临时引擎检查脚本、运行器哈希、启动参数和精简崩溃证据。 |
| `qa/t01/maker-build.json`、`qa/t01/continued-verification.json` | 续验的 Maker 同步阻塞、恢复、构建结果、版本与源码核对。 |
| `qa/t01/chrome-final-modules/`、`qa/t01/chrome/` | 最终源码对应的 6 组模块结果、LSP、9 段实际运行录像、脱敏引擎状态与核对清单。 |

`Data.lua`、`main.lua`、交接包和引擎库未修改。`.project/project.json` 临时使用隔离验收入口，结束时已恢复 `main.lua`；相对基线只剩末尾换行差异。

## 正常、取消、无效、重复与恢复路径

| 操作序列 | 状态、记录、UI 绑定及存档的实际结果 |
| --- | --- |
| 老木尺快查 → 年度推进 → 修复解锁营造图 → 保存 → App 重启 → 新立家谱 | 收藏中营造图仍解锁；原终章档案夹具保持不变；新局选入老木尺与营造图后 99/100，只有所选物件成为本局实体；开始前草案和新局快照一致。 |
| 新局确认弹窗取消；或“取消新局，回到原家谱” | 弹窗取消不写文件、不动旧 run；返回旧局恢复原草案。新草案不会因为浏览或渲染替换旧局。 |
| 重复点击旧确认回调；超分草案开始 | 重复确认不再次创建；超分不会进入覆盖确认或写入。 |
| 第 151 条日志后 → 家史 → 最早 → 上一页/下一页 → 重启 | 151 条全部存在，第 1 条位于第 8 页；界面回调可找到首条；翻页不改 run 或 RNG；新 VM 重启后仍可找到。 |
| 年度真实离世产生唯一成年候选 → 家族事件页任命 | 前任任期闭合，新任期唯一；事件 resolved 且记录成员和年份；“推进这一年”恢复；重启后可推进到第 2 年。 |
| 同样输入 → 人物页“任命为族长” | 与事件入口产生相同任期、待决状态和一次性声望/日志；死亡/不存在/未成年候选被拒绝；重复任命不增加奖励或记录。 |
| 旧档已任命但继任事件仍 pending → 确认现任 | 只关闭遗留事件；任期、声望、日志、费用、RNG 不重复变动。 |
| 无旧档首次短写 → 再次开始 | 候选留在内存，首次写坏的候选在同一进程可重试；成功后才进入游戏。 |
| 有旧局，新局写入遇打开失败/短写/返回 0/回读坏内容/写入异常 | 新局不替换内存旧 run，展示草案保留，错误提示不被成功通知覆盖；上次可读档保持可恢复；重启从保留档恢复。 |
| 接济邻里已扣 15 两 → 保存短写 → 再点安排/读取/新局 | 当前内存为 65 两、援助 1 次，旧档仍 80 两；常驻“未保存”错误和重试入口，暂停进一步安排/覆盖。导出可保留当前进度副本，但不解除未保存状态；重试保存成功后，内存、保存、日志和 UI 绑定一致，没有重复扣款。 |
| 一份存档坏；两份都坏；外部恢复文件后重新读取 | 单份坏则读取另一份并提示核对年份；两份都坏阻止默认新局覆盖；恢复文件后可读取并正常保存。 |

## 直接模块测试与检查

最终执行：`/private/tmp/jiaye-t00-venv/bin/python tests/t01/run.py --out qa/t01/chrome-final-modules`。

| 组 | 最终结果 |
| --- | --- |
| history | PASS |
| succession | PASS |
| collection | PASS |
| failed_new_run | PASS |
| failed_action | PASS |
| recovery | PASS |

合计 **6/6 PASS**。五个生产 Lua 文件先前的 EmmyLua ERROR 数均为 **0**，后续改动的 App.lua 和新增 QA/T01.lua 再检仍为 **0**；差异空白检查通过。最终文件哈希见 `qa/t01/chrome-final-modules/manifest.json`，收尾时逐项核对与当前文件一致。清单记录的 base SHA 是测试时 HEAD，实际被测工作区文件由哈希确定。

第一轮 3/6 的失败来自测试边界：Box 数值参数适配、Lua 多返回值传给 Python JSON；已修正，保留 `attempt-1` 原结果。后续回归和首次写入重试测试见 `attempt-2`、`attempt-3` 和 `final`，不删失败证据。

此处的 UI 证明是**组件声明与真实回调绑定**，不是 Yoga/NanoVG 绘制；File 证明是**磁盘适配与独立 Lua VM 重启**，不是 UrhoX 用户隔离目录或真实 cjson。原 8 探针仍是历史复现材料，本步只把 P01/P07、收藏覆盖及相关恢复合同迁入正式直接模块测试，没有将其他历史缺陷改成“预期失败即通过”。

## 早期引擎尝试与构建记录（保留原失败事实）

从工程自带安装器指定的官方地址下载了 macOS UrhoXRuntime，放在 `/private/tmp/jiaye-t01-runtime`；没有更改已绑定 Maker 工程。尝试用独立 `/private/tmp/jiaye-t01-engine/savedata` 测试真实 File/cjson，两个本地调用均退出 **139 / SIGSEGV**，栈顶为 `Urho3D::LoadingUI::Show()` → `BootstrapManager::Start()` → `UrhoXRuntime::StartGame()`。没有 `T01_ENGINE_FILE_PASS` 或 `T01_ENGINE_FILE_FAIL` 标记，隔离存档目录为空。

这只证明这些本地启动调用未成功，不能据此判断游戏 Lua 存在运行错误，也不能声称远端 Maker 引擎失败。临时帮助进程已清理；没有擅自报告外部 Issue 或更改引擎实现。

### 2026-09-16 续验：Maker 构建已通过

1. 按已安装 `taptap-maker-local` 工作流使用 `maker_build_current_directory`。第一次在提交前被拒绝：本地 ahead 17、behind 15，未创建修复提交，也未开始构建。
2. 只读比较确认本地基线与 Maker 远端 `605b99e` 的文件树完全相同，tree 都是 `e4ca1fae81e296f842d79ea1fff543e1f3df88ca`；分叉来自重复提交历史，未发现需选择保留的代码差异。通过普通 merge 保留两边历史，生成 `48a899f`，没有重置、变基或丢弃工作区内容。
3. 验证 T01 生产文件和测试文件与 `qa/t01/final/manifest.json` 哈希一致，再由 Maker 工具仅提交三个生产 Lua 文件。修复提交 `ca5b5f2` 推送成功，远程构建成功，预览刷新 HTTP 200。测试、报告和原交接目录仍保留在本地，未混入本次 Maker 提交；未单独推送 GitHub remote。
4. 本地与 Maker origin/main 已对齐。日志 watcher 已启动，读到的状态无轮询错误；尚无玩家运行日志，不能据此声称玩法已运行或通过。
5. 当时在 Codex 内置浏览器打开预览链接后重定向至登录介绍页，实际交互被登录阻塞。随后用户明确授权使用已登录的 Chrome，此阻塞已解除；没有代填凭据或绕过鉴权。

预览入口：<https://maker.taptap.cn/app/4e7e4ee8-41b8-4dbb-9b5d-24831ca14273?localDev=1>。

## 2026-09-16 Chrome 实际引擎验收

通过用户授权的 Chrome 操作 Maker 托管 UrhoX 运行版。生产页面以真实 Yoga/NanoVG UI 绘制，隔离入口使用引擎 File/cjson；通过 Maker 自带录屏按钮下载原始 MP4，没有合成图片充当 Demo。所有录像时长、尺寸与 SHA256 见 `qa/t01/chrome/verification.json`。

| 路径 | 实际结果 | 录像（位于 `qa/t01/chrome/`） |
| --- | --- | --- |
| 新立家谱 → 开始确认 → 取消 → 返回原局 → 手动保存/重新读取 | 顾氏第 6 年、7 两、9 条家史保留，出现回读核对成功提示；确认弹窗底色修复后文字可读。 | `02-confirmation-and-save-after-fix.mp4` |
| 151 条家史 → 最早 → 前后分页 → 重启预览并读取 | 第 8 页显示第 1 条，重启后仍有 151 条，RNG 仍为 72831。 | `03-history-151.mp4` |
| 实际年度离世 → 事件入口任命唯一成年候选 → 推进 → 重启读取 | 许青继任；前任 endYear=1，新任 startYear=1，事件 resolved/memberId=2/resolvedYear=1；推进至 yearIndex=2，重启保留。 | `04-succession-event.mp4` |
| 同样离世条件 → 人物页任命 → 推进 | 与事件入口的任期和待决收束一致；修复成员弹窗后重测，可读且可操作，推进至 yearIndex=2。 | `08-succession-member-after-color-fix.mp4` |
| 老木尺调查/修复实际解锁营造图 → 新局选择两件 → 确认 → 重启读取 | 收藏仍解锁 plan；草案和新局均为 99 点，实体仅 ruler/plan 两件；新局 money=0，重启保持收藏与实体。 | `06-collection-new-run.mp4` |
| 一次短写 → 点击推进 → 重试保存 → 重启读取 | 内存 65 两/援助 1 次/日志 1 条，旧档 80 两；未保存提示阻止继续安排；重试后存档为 65 两，不重复扣款，重启保持。 | `07-short-write-retry.mp4` |
| 恢复正式 main.lua 并构建 → 读取原档 → 家史 | 仍为顾氏、族长顾安、第 6 年/大晟历 53 年、4 人、7 两、22 石、0 亩、0 声望、9 条家史，最早 48 年迁居记录仍在。 | `09-main-restored-original-save.mp4` |

`engine-run-02.jsonl` 含 137 条脱敏运行日志，其中 44 条实际 App.Render 状态观察；除故障注入期间外，全部 shown/saved 摘要逐项相同。该摘要含任期、事件、信物实体、RNG、钱粮、年份、家史数量/最早记录；不是全量文件字节比较。完整状态不变及重复命令边界另由直接模块测试覆盖。关键行号和核对结果保存在 `verification.json`。预览刷新出现新的 `T01_QA_READY` 后，再读取旧隔离进度，证明本次引擎重启后的读取路径。

短写是验收入口一次性把真实 File 写入截为 12 字符，未伪装成实际磁盘故障。异常返回/回读错误/首次保存/坏档/重复确认/非法任命等其余边界在直接模块层验证，没有逐一声称为浏览器实测。

保留了两段显示缺陷修复前录像 `01`、`05`。首次收藏录制期间发生两次预览重新启动（日志时间 1789557503、1789557549），原因未定位，受中断录制不计通过；随后完整重复并核对重启读取通过。本次捕获日志未见 Lua ERROR 或 `T01_QA_FAIL`，这不等于已排除所有运行稳定性问题。

## 尚未验收或后续步骤

- 手机触控、安全区、软键盘、窄屏长列表、原生应用退出/重启与真机存储生命周期；桌面 Chrome 中的实际引擎证明不能替代真机。
- 本地 macOS Runtime 的两次启动失败与上述预览中断原因仍未定位；没有把它们改写为通过。
- 两份文件均损坏时的完整导入恢复、V5 迁移、完整结构/引用校验、断电原子提交仍属 T10。当前双档保障保留上次可读副本，不声称已经具备完整事务持久化；完整写入后若回读失败，重启可能读到该完整候选，必须核对年份。
- 已终局档案保持的证明来自直接模块夹具；本次未在真实引擎自然达成终章。
- T02–T11 的其他已知缺陷与新开局目标尚未修复，本步不增加总预算，不改价格，也未增加新世界/信物。

按 T01 的交付与验收范围，本步已完成。下一步依赖为 T02，本次未开始；整个产品及手机体验不作通过结论。
