# T08｜十三条终章与只读

基线提交：`982abfc153612f68cd4a093684f7e0e25114bd2b`。

## 终章定义

`Data.Endings` 登记十二条发展终章与一条自动终章：

| 类型 | 数量 | 写入时机 |
| --- | ---: | --- |
| 发展终章 | 12 | 条件具备后，由玩家在确认页落笔 |
| 自然终章“家谱落笔” | 1 | 年度结算后全体在世族人归零 |

每条条件行都保存条件名称、当前数值、目标数值、数值类型与事实来源。数值类型使用当前、累计、连续、代际累计、历史；事实来源指向年度账本、族长任期、家谱人物、迁居记录、援助记录、信物经历或公库。

## 正常路径

1. 发展终章达标后继续推进年度、安排人物或处理事件；资格本身不会结束本局。
2. 家主点击“以此落笔”后先查看条件快照，选择“继续经营”可返回游戏。
3. 确认落笔后，`Simulation.FinalizeEnding` 统一写入终章 ID、年份、族长、条件快照、已具备的发展终章、待决事件关闭清单和终章事实。
4. 当前局与 `profile.endingRecords` 保存同一份快照；家史、首页与存档读取同一份终章记录。
5. 自然终局复用同一提交入口，条件为在世族人归零，自动关闭尚未处理的事件。

## 边界与失败路径

| 情况 | 结果 |
| --- | --- |
| 未达成发展终章 | 拒绝写入，当前局和档案保持原状 |
| 试图确认自然终章 | 提示自然终章由全员离世自动写入 |
| 同年更换族长 | 任期未满一年，不计入真实交接 |
| 婚入同代成员 | 继承配偶代际，不增加传承代数 |
| 选择不同合法开局年份 | 实际经营年数从 `yearIndex` 计数，开局年份不计入 |
| 终局后安排人物、家业、事件或信物 | 领域入口拒绝，页面只展示记录 |

## 页面状态

家族页显示本局终章、自然或确认状态、条件快照与“新立家谱”。族人页改为查看生平；家业页展示封存的公库和迁居记录；信物页展示封存的物件状态、保管人和调查记录。家史页同时显示本局终章快照与十三条原型的现况。

## 真实引擎 QA

入口：[scripts/QA/T08.lua](../scripts/QA/T08.lua)。入口只创建正常生成的合法家庭，调用生产 `State`、`Opening`、`Simulation` 与 UrhoX UI；不读取、写入或替换玩家存档和引擎接口。

覆盖内容：

1. 十三条登记与自然终章定义。
2. 实际年度经营获得发展终章资格，继续经营后确认落笔。
3. 全员自然离世后的自动终局、档案快照、存档编码读取和事件关闭。
4. 终局后所有运行期写命令保持状态不变。
5. 同年交接、同代婚入和合法开局年份边界。

当前静态诊断：Maker Lua LSP 对 8 个 Lua 文件报告 `0` 个 Error。`git diff --check` 通过。

已在 Maker 远程构建提交 `81635060e5858aaf66278a43bad2a5ae76620147` 执行 `QA/T08.lua`：构建成功、预览刷新返回 HTTP 200，运行监视器连续失败数为 `0`。本地 Chrome 中的 UrhoX 预览显示“`T08 真实引擎验收通过`”，运行日志记录 `T08_QA_PASS`；发展终章的继续经营与确认落笔、自然终局的全员离世和事件封存、终局写操作只读、同年交接、同代婚入、合法开局年份边界均通过。

主入口已在本地 Chrome 的 `390×844` 竖屏预览运行：默认生成的家庭摘要首屏同时显示家名、钱粮、田地、声望、人口入口、信物、首年净变化、总计分数，以及“换一家”和“就从这家开始”。构建提交 `5b7ffff` 的 `main.lua` 远程构建成功，预览刷新返回 HTTP 200，运行日志确认《家业》主入口已启动。

本地 Chrome 已生成 `0:22` 的预览录屏，画面覆盖封面和随机生成的紧凑开局摘要。该录屏是浏览器预览证据；终章的状态、记录、只读和存档编码一致性以真实引擎 QA 画面与运行日志为准，未将浏览器预览表述为真机证据。
