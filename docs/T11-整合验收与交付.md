# T11｜整合验收与交付

功能源码提交：`70cb2ae918d64c5f75ab4ba0a64fa7690d7c760c`；最终远端正式入口提交：`7337698`。本步将当前开局、年度经营、家主交接、终章、历史、保存和重载放进同一条真实 UrhoX 流程验证，并重新构建主入口。

## 修改文件

| 文件 | 内容 |
| --- | --- |
| `scripts/Jiaye/Simulation.lua` | 成长提醒找不到参与族人时清除运行期成员引用，保留作废对象记录，保存校验可以继续保护有效家谱 |
| `scripts/Jiaye/Data.lua` | 登记全员离世与钱粮同时归零两条自动终局，以及可回看的触发标识与条件说明 |
| `scripts/Jiaye/App.lua` | 家族页预警钱粮同时归零风险，家史账本与终局状态显示对应事实 |
| `scripts/QA/T11.lua` | 隔离保存文件的真实引擎整合验收入口 |
| `tests/t00/audit.lua` | 对齐当前 UI 组件、保存格式、确认开局、家史分区和交接行为 |
| `tests/t00/run.py` | 历史版本缺少新增源码时记录缺失状态，审计可继续生成证据 |
| `tests/t06/contract.lua` | 对齐人物经历和家史分区的现有文案与入口 |
| `.gitignore` | 忽略本地 `qa/t11/` 结果 |

## 连续流程

`QA/T11.lua` 使用真实 `File`、`cjson`、`UI` 与生产模块，仅将 `jiaye_save.json`、备用保存和导出文件改到 `t11_qa_` 前缀。

1. 从生成器选取合法的完整家庭，创建运行家谱并保存；浏览家庭页。
2. 全部在世成年人安排务农，关闭生育计划，连续经营十年；每年清理待决家事，第一年执行一次家主交接。
3. 获得安稳终章资格时继续经营数据仍可读；确认终章后写入终章条件、档案和事实。
4. 终章后尝试推进年度被拒绝，运行家谱逐项保持不变。
5. 保存、导出、预演导入，重新创建 `App` 后浏览家庭和终章家史；终章、档案、账本、事实与保存修订号来自同一份内容。
6. 将一户在世家人的公库、存粮和田地置零后完成年度结算；账本写入钱粮归零，自动终局关闭家事，人物生平、终章档案和重载记录保持一致。

实际运行记录为：家族“叶”，2 名成员，经营 10 年，17 条事实，10 本年度账，终章 `peaceful`，保存修订号 3。

## 失败路径

| 输入或状态 | 行为 |
| --- | --- |
| 生成器未产出合法整户 | 返回生成失败信息，当前草案保持不变 |
| 成长提醒引用的族人已不存在 | 事件标记为作废，旧对象写入 `cancelledMemberId`，运行期引用清空，后续保存继续执行严格引用校验 |
| 终章前未满足条件 | 终章请求拒绝，不写入终章、档案或条件快照 |
| 终章确认后继续推进年度 | 操作拒绝，运行家谱逐项保持不变 |
| 年度结算后公库与存粮同时归零 | 自动写入“家道散尽”，待决家事关闭，人物既有经历保留可读 |
| 年末钱或粮仍有一项大于零 | 不触发“家道散尽”，运行家谱继续可安排 |
| 导出内容校验或预演导入失败 | 当前资料、草案、运行家谱和磁盘保存保持原样 |

## 直接模块验收

| 入口 | 结果 |
| --- | --- |
| `tests/t00/run.py --out qa/t11/completion-20260917-t00` | 16/16 PASS |
| `tests/t01/run.py --out qa/t11/completion-20260917-t01` | 6/6 PASS |
| `tests/t02` 至 `tests/t07` 合同入口 | 6/6 PASS |
| `tests/t08/run.py --out qa/t11/completion-20260917-t08` | 2/2 PASS |
| `tests/t10/run.py --out qa/t11/completion-20260917-t10` | 5/5 PASS |
| `tests/opening/run.py --out qa/t11/completion-20260917-opening` | 4/4 PASS |

直接模块结果共 39 PASS。Lua LSP 在最终构建前报告 `errors: 0`、`warnings: 213`。

本次复核输出保存在本地忽略目录 `qa/t11/completion-*`，其中 T00 为 16 条、T01 为 6 条、T02–T07 为 6 条、T08 为 2 条、T10 为 5 条、开局为 4 条。

## Maker 与 Chrome 证据

Maker 以 `QA/T11.lua` 构建至 100%，运行日志连续两次记录：

```text
T11_QA_PASS 生成家庭、年度交接、终章、家道散尽、只读、保存与家史重载通过
```

该入口实际生成叶氏 2 人家庭，经营 10 年，写入 17 条事实与 10 本年度账；发展终章为 `peaceful`，家道终局为 `collapse`，自动触发标识为 `resources_exhausted`。它确认终局后写操作保持只读，导出、预演、重载后的终章档案、账本与家史仍来自同一份保存。

随后以 `main.lua` 构建至 100%，预览刷新返回 200。Chrome 的临时 `390×867` Maker 预览显示当前纸面封面，项目配置同时显示入口为 `main.lua`。本次没有新增录屏。

当前源码还以真实生成家庭重新核对了 360×844、390×844、430×844 三种竖屏宽度。家事页、族人页、家业页、藏阁页和家史页均可见；长姓名在成员卡内换行，底部导航和“推进这一年”保持触达。该项使用 Chrome 的设备尺寸预览，已在结束时恢复默认窗口尺寸。

运行日志包含 Maker 资源下载解析警告。隔离 T11 入口有两次通过记录；最终 `main.lua` 构建完成后，本地日志监视器因旧进程未在 1.5 秒内退出而未重启，Chrome 预览确认正式入口与封面已加载。

## 验收边界

本轮以直接模块、真实 UrhoX 运行入口、Maker 构建和 Chrome 设备尺寸预览作为验收范围。物理手机触摸、软键盘、安全区和本机玩家历史保存不纳入本轮交付。
