# T02–T05：随机开局与数值透明

## 范围与当前结论

本轮基线：`ca26af6cb6fb9dc9c18ca747f8f1de78edc9bb86`（T01 后）。原历史评审固定在 `546ff6cf7e4225632fe9871ba70188e6aa730e82`；本轮依据实际源码重新检查，不把历史探针结果直接当现状。T00/T01 报告与证据仍保留。

用户对“整户生成、三种随机与姓名、透明数值、紧凑摘要、可选五步编辑”批注“行，你来做这些”，本轮因此落实 T02–T05 关联内容。延续 Lua/UrhoX、凡世与原有价格；原 97 点林家保留为兼容/测试夹具，玩家新局走生成器。

**代码、直接模块验证和 Chrome 中的真实 UrhoX 录屏验收均已恢复。** 当前证据来自桌面 DPR=2 环境下的逻辑 frame 重排，不等同于真机验收；不能将它替代触控、软键盘或安全区验证。

## 修改路径与行为

| 路径 | 内容 |
|---|---|
| `scripts/Jiaye/Opening.lua` | 整户生成、命名绑定、三个随机命令的作用域、按页快照、人物候选校验 |
| `scripts/Jiaye/Economy.lua` | 从年度逻辑提取同源收支内核；预估在副本运行，给出钱粮逐项账本 |
| `scripts/Jiaye/State.lua` | 同源计分明细、合法性/100 点边界、初始独立声望效果相加、开局快照 |
| `scripts/Jiaye/Data.lua` | 现有价格常量和岗位/偏向展示索引；不改原价格 |
| `scripts/Jiaye/Simulation.lua` | 年度调用同一个经济内核并保存 `lastLedger` |
| `scripts/Jiaye/OpeningView.lua` | 紧凑摘要、全量明细、可选五页编辑、人物临时副本 |
| `scripts/Jiaye/App.lua` | 草案命令、取消/保存、按页撤销、开始校验与原局保护 |
| `scripts/main.lua` | 启动说明更新；保持正式入口 |
| `scripts/QA/Opening.lua` | 真实引擎隔离验收入口，使用 `opening_qa_` 存档前缀；正式入口不引用 |
| `scripts/QA/T01.lua` | 旧 99 点收藏场景明确使用冻结夹具，避免玩家默认随机后夹具失真 |
| `tests/opening/*`、`tests/t01/{run.py,boundary.lua,cases.lua}` | 新合同验证与 T01 回归适配，不使用历史 8 探针作为正式验收 |

## 正常路径

- 新玩家生成 2–6 人家庭，人口、年龄、关系、潜力、本领、合法安排、背景、资产、信物均来自种子生成；48 个样本合法且有实质差异，另有固定六人家庭夹具。相同输入和种子可复现，不承诺每次唯一。
- 摘要读取同一草案，成员/信物/计分/收支明细只读。开始创建 `openingSnapshot`，与正在展示的草案深度一致，保存回读后才切入新局。
- “换一家”替换整户候选，可恢复上一户；“姓名骰子”只推进独立命名种子；“本页随机”只改对应字段与生成种子。
- 家族改名只同步 `nameSource=family` 成员的名，不猜测字符串前缀；外姓即使与家族同姓也保持，手改完整姓名改为独立保留，复姓按 Unicode 字符计长，零点。
- 可直接编辑某人，或进入世道/族人/家底/信物/确认五页。新增人物先留临时副本，保存才占编号；取消不改变草案。姓名输入不重建整棵 UI 树。
- 每页随机各存一份前后字段快照，撤销仅恢复该页；当前页手改使旧撤销失效。取消整次编辑恢复进入时草案。
- 计分共用 100 点，无类别配额，不要求花满。信物仅从已解锁池选，保留多选预算；没有新增示例罗盘。

## 失败与边界

- 100 点合法开局；101 点、非法世界/年龄/岗位/亲缘/婚配/信物不能开局，失败不改原草案。
- 分页随机无合法组合返回错误，保留原成员、资质、家底和种子；不偷偷降级或删人。
- 有旧局时，试配/浏览/取消不写旧存档；确认开始前一直保留旧局。重复确认不重复创建。
- T01 保存失败保护仍回归：失败保留上次可读进度，重试不重复扣费；重新启动独立 Lua VM 读回新局草案和账本。
- 原经济价格、收入、培养、生活与粮食取整沿用；修正旧日名门按全户每年加一次声望，来历/往来关系初始效果相加；断粮年份不计稳定，连续丰食计数失败归零。未宣称 T06 全部人生指标已完成。

## 验证层级与验收记录

生产 Lua 5.4 模块合同仍覆盖模型、经济、UI 回调、开始/保存/重启集成；T01 回归保留独立 Lua VM 的保存与读回边界。模块合同证明业务与回调合同，不证明 NanoVG 排版、触控、软键盘或真实引擎存档。

Chrome/真实 UrhoX 录屏与日志现已提供 360×800、390×844、430×932 三种逻辑 frame 的重排证据。运行桌面物理尺寸为 3728×2370、DPR=2，故这些是桌面逻辑尺寸，不声称已完成真机屏幕适配。`qa/opening/chrome/verification.json` 固化了 11 项已发生的运行时审核证据；视频 01–08 已保存：07 证明最终加减控件与 360 首屏关键数值完整可见；08 证明本页随机从 94 点变为 82 点、撤销回 94 点且钱粮田保持 60/4/2。

正常路径已验证：同一 seed 的 360/390 草案一致；430 使用独立 seed722 的六人、七信物池场景，因此仅用于布局而不作为跨尺寸草案差异。手动名字编辑为“欧阳自定”后，改家族为“司马”只更新家族成员姓名，外姓保留；地区下拉仅将一项从 98 点变为 100 点。真实开局中展示 draft、`openingSnapshot` 与隔离 saved 一致；隔离读取恢复旧开局，首年由 50 钱/0 石结算为 65 钱/8 石，账本与生产 `Economy.Preview` 同源一致，重载引擎后仍读回 `yearIndex=1`。

失败与取消边界已验证：作坊使草案从 100 点暂时超至 114 点，取消后恢复 100 点；money=130 的 102 点草案“开始”控件禁用，隔离 save 未被覆盖。新增成员后取消不写入草案。

数值控件修复已通过构建 `5660579` 并完成真实复测：`OpeningView` 将误用的 Stepper 改为 `+/-` 44 按钮，仅改变展示层交互，不改经济逻辑。钱 50→60、粮 0→4、田 3→2、点数 96→94，年龄 38→39 后可保存。

直接模块最终为 4/4 通过（`qa/opening/final-controls-modules`），T01 回归 6/6 通过（`qa/opening/final-t01-regression`）；模块清单记录源码哈希。LSP 所检查文件无错误。

剩余验收：真机 IME、触控和安全区仍须独立验证。T06–T11 未因本轮证据而宣布完成。
