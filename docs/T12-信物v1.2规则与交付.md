# 信物 v1.2 实现与运行记录

依据：用户提供的《家业-美术需求与信物完整方案-整合包》中《家业-七信物数值与完成逻辑-完整方案-v1.2.md》及配套美术说明。工作在现有 Lua/UrhoX 工程内完成，使用 5 个 Luna 子任务分工，主任务负责集成和真实引擎验收。

## 六步实现

| 文档步骤 | 工程落点 | 实际实现 |
| --- | --- | --- |
| 1. 身份与版本 | `RelicDefinitions.lua`、`RelicState.lua`、`RelicValidation.lua` | 六条成长线、20 种形态、27 项行动；补谱保留原实例；主件、收藏资格、曾获得记录和实际物件分别保存；新开局使用 v1.2，旧局保留旧规则 |
| 2. 实际使用与任务 | `RelicSystem.lua`、`Economy.lua`、`Simulation.lua` | 保管人、使用者、执行人独立；年初锁定资格；真实执行与支付形成使用记录；同件同年去重；一人一件额外事务；封存、暂停、接手、待决、取消、出售及死亡处理 |
| 3. 木尺与营造图 | `RelicOutcomes.lua`、`RelicSystem.lua` | 查匠号、誊图、购图、修缮、升阶、传艺、修桥；项目接受时固定挂载物件，完工依据真实工作年度与参与人分别记工程事实；本金、净报酬和年度收入分开记账 |
| 4. 族谱、家书、玉佩 | 同上 | 缺页查证与同实例补谱、固定旁支候选、真实归家与交接、送信与履约、有限援助、玉佩互斥终形、接入实际迁居优惠；首章及奖励领取去重 |
| 5. 医案与跨系统 | `RelicOutcomes.lua`、`Habits.lua`、`Economy.lua` | 实际行医与学医分开；病例、家传、公开刊行、义诊；跨代使用按真实代际与前后年度；连续三年实际读书形成“不废灯火”，暂歇、失效和恢复按文档结算；外部工程登记、人物冲突及证明接口已提供 |
| 6. 页面与存档 | `RelicV12View.lua`、`App.lua`、`OpeningEditor.lua`、`HistoryView.lua`、`State.lua` | 开局按已解锁形态计价；藏阁图鉴、行动、作用/成长/经历、门槛与报价、参与人和挂载选择；保存、读取、备份校验保留同一任务与实例 |

## 页面与美术

- 使用既有 `urhox-libs/UI`，正式入口为 `main.lua`。
- 依照 game-ui-ux 的内容滚动与固定操作区规则安排弹窗；主操作保持可点击尺寸。390×844 浏览器视口中已实际点击图样行动、办理人、木尺挂载和确认。
- 保留现有 v1 资源解析、固定人物 `artId`、年龄/状态图、家宅、事件、按钮和图标。新行动按文档映射复用相应插画。
- 高阶形态使用正确阶名与“临时复用基础图”提示。整合包中的水墨综合参考图作为方向参考；20 种形态的独立精修图、字体授权文件、PSD 和原生 Figma 工程均未包含在包内。

## 2026-09-20 运行证据

### 真实 UrhoX 引擎

`scripts/verify-v12.lua` 调用正式业务模块，以合法开局走完：木尺查访 → 暂停 → 保存与读取 → 换执行人接手 → 校准原尺 → 取得营造图 → 挂载尺与图完成修缮 → 实际使用积累 → 增订 → 保存与读取 → 导出结构校验。

验证采用专属 `jiaye_v12_verification.json` 及其备份/导出文件，正常入口不启用该文件组。未通过预写使用年数或工程事实跳过门槛，未覆盖用户正常进度。

20:43 的真实引擎日志输出完整路径通过标记。验证后 UI 显示验收氏第 6 年、增订的营造图、两条工程证明以及既有任务历史。真实文件写入与读取返回的实例、阶级、解锁资格和暂停任务 ID 均通过断言。

手机浏览器交互中，修缮显示现在支付 8 两、一个工作年度后到账 18 两、净报酬 10 两。选择许青并挂载校准木尺后，点击确认，现银由 251 两降至 243 两；页面回到藏阁，关联物件显示进行中 0/1 年。详情底部行动按钮可完整触达，经历页显示中文家史与实际使用记录。

### 业务与静态检查

使用正式 Lua 模块执行短路径，确认暂停不自动完成、换人接手、原尺升级、誊图只扣一次费用、重复确认不发奖励、项目实际到账与预估一致、封存期间停止图样年度收入、冷却恢复、暂停工程出售挂载物后取消、出售后停止收益、旧规则年度结算，以及补谱同实例、家书履约和赠玉。

全工程 Lua LSP 检查为 0 Error。检查未扩展为全量回归；27 项行动均已配置并接入，未声称每项都经过独立浏览器通关。

## 明确保留的边界

1. 六个背景机会的现有配置仍缺价格与完整结果；v1.2 第 11.2 节要求沿用既有价格和奖励。已提供共享人物冲突与外部工程事实接口，六项机会尚未形成可玩的完整流程。
2. 水墨独立精修终稿尚未包含；本轮使用已交付 v1 图像与文档允许的高阶复用。
3. 本机备份文件写入、读取和导出结构校验已执行。游戏剪贴板返回写入成功，跨应用剪贴板读取为空，外部复制与跨设备导入尚未验证。
4. 已执行浏览器内真实 UrhoX 运行和手机尺寸页面检查，物理手机运行未执行。

## 入口与验证方式

正式构建选择 `main.lua`，使用玩家正常存档。需要复现上述短路径时，构建选择 `verify-v12.lua`；此入口使用独立验证文件，完成后需切回 `main.lua`。验证入口每次启动重新生成专属验证进度。

提交批次：`6bd45d5` 为 v1.2 规则与页面，`8b0ff66` 为独立存档真实引擎验证，`4ca9121` 为手机详情与事实展示。后续正式入口构建包含本文件与年度显示校对。
