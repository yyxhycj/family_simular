# T02｜统一草案、价格、预计收支与随机作用范围

状态：**功能验收通过（直接模块、保存回读与正式 Maker 构建）**。这只表示 T02 规定的草案、预算、预估、效果叠加与本页随机事务完成；不把 T03 的整户生成、T04 的手机摘要、T05 的完整编辑器混作本步结论。

## 基准与范围

- 历史评审提交：`546ff6cf7e4225632fe9871ba70188e6aa730e82`，仅作缺陷对照。
- 本步构建提交：`6a2a191d1f54885d9a3e0dcd410ffd53a633453a`，`main.lua`，Maker 远端构建成功。
- 保持 Lua/UrhoX、凡世“大晟”、纯文字家族循环。没有增加世界、地图、建筑玩法或修改经济平衡。
- 开局点数与运行中的银两刻意分开：前者使用 `Data.OpeningCosts`，后者使用新增但数值不变的 `Data.RuntimeAssetCosts`。

## 本步修改

| 文件 | 改动 | 影响 |
|---|---|---|
| `scripts/Jiaye/Data.lua` | 增加运行期置办价表：田 30、作坊 80、商铺 120 两 | 保留既有数值，成为唯一来源 |
| `scripts/Jiaye/Simulation.lua` | `BuyAsset` 读取运行期价表 | 结算动作不再内嵌第二份价格 |
| `scripts/Jiaye/App.lua` | 家业页按钮读取同一价表 | 显示与扣款不会漂移 |
| `scripts/Jiaye/State.lua` | 开局家底要求非负整数；现银只能按 10 两、存粮按 4 石；信物/产业类型错误均拒绝开始 | 非法草案保留原样，不取整、不删人、不降资质 |
| `tests/opening/economy.lua` | 增加效果叠加、价格与非法草案断言 | 保留综合回归 |
| `tests/t02/*` | 新增只覆盖 T02 的直接模块合同与运行说明 | 单独产出可审计结果 |

`Opening.Fields` 仍是本页事务的正式范围：`world` 为世界/时期/年份/来历/地点，`people` 为成员/族长/编号，`estate` 为资产/住宅/产业/家风/关系，`relics` 为选中信物。`Opening.Snapshot/Restore` 只读写该页字段；姓名、整户、本页随机仍由不同命令处理。

## 正常与失败路径

| 场景 | 输入与实际结果 |
|---|---|
| 预算与开始 | 现银 110 的固定夹具为 100 分，`NewRun` 成功并将展示中的草案写为 `openingSnapshot`；现银 120 为 101 分，被拒绝。 |
| 非法资产 | 现银 111 被拒绝；草案深比较保持相同，没有自动取整。 |
| 同源预估 | 默认夹具预估收入 29、净银 -1、净粮 +5；与同状态 `Settle` 一致，预估前后的 Run 深比较相同。 |
| 独立作用叠加 | 旧日名门 + 乡里相熟开局声望为 37；商埠 + 行商后裔 + 商路故交的一位经商成员收入为 36。 |
| 运行期置办 | 购田从同一价表读 30 两，扣款后田地加 1。 |
| 本页随机与撤销 | 家底随机后修改世界页，再撤销家底：世界页手改保留；家底页手改后旧撤销记录失效。 |
| 保存与回读 | 综合合同先保留旧 Run 至确认，确认后保存 `Run` 与 `draft`；年度账本写入后重建 Lua VM 回读，草案快照、年度与账本一致。 |

## 直接模块结果

执行环境为固定 `lupa==2.6` 的 Lua 5.4；仅替换 UI 声明与 File/JSON 边界，不替换游戏规则或随机源。

| 命令 | 结果 | 证据 |
|---|---|---|
| `tests/t02/run.py --out qa/t02/final-3` | 1/1 通过 | `qa/t02/final-3/results.json`、`manifest.json`；源码哈希与本步提交相符 |
| `tests/opening/run.py --out /private/tmp/jiaye-opening-after-build` | 4/4 通过 | 模型、经济、UI、集成；集成含确认前旧档不覆盖、保存后重启回读、同源账本 |
| Maker Lua LSP | 0 错误 | 7 个文件的 124 条诊断中为 0 errors；其余为既有 warning/hint |
| Maker `main.lua` 构建 | 通过 | 远端构建 100%，日志记录《家业》已启动 |

第一次 T02 合同因测试夹具把“名门 + 乡里关系”叠到默认 97 分，成为超分而失败。该失败留在 `qa/t02/final/`；已将夹具改为合法、不满 100 的家底后重跑，最终结果保留于 `qa/t02/final-3/`。

## Chrome 与录屏

本次正式 Maker 预览在 Chrome 启动至《家业》封面，运行日志无 Lua 报错。这个新建的 Chrome 测试页没有携带先前浏览器的顾氏存档，所以不以它宣称旧档回读成功。

本步新增的是规则来源与非法数据拒绝，正常画面不变，因此没有把旧录屏冒充为当前提交的新增功能证据。既有 [本页随机/撤销录像](../qa/opening/chrome/08-page-random-undo.mp4) 仅保留为交互历史参考；当前提交以直接模块的状态、记录、UI 回调与存档回读合同，以及正式构建启动日志为准。

## 尚未通过或不在本步范围

- **RV-A17 未完全结案：**运行期购置价格已同源并保持原值；手动购粮、购售粮完整入口、住宅购买与其预估展示仍属于 T06/T09，不能声称已实现。
- 真机触控、中文输入法/软键盘、安全区未验证；Chrome 是桌面预览，不是真机证据。
- T03、T04、T05 及 T06–T11 不因本报告变为完成。
- Maker 日志仍有资源 UUID 解析 warning；本次纯文字入口继续启动，日志没有对应 Lua 运行错误。资源告警需在涉及资源资产时另行处理。

按 T02 的验收边界，状态（草案与 Run）、记录（年度账本）、UI 回调（随机/撤销/错误就地显示）与保存回读已有同一源码清单下的直接合同；本步完成。
